name: tankiq
description: "Aplicación para la administración y control de volumenes en terminal de hidrocarburos."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  web_socket_channel: ^3.0.1
  url_launcher: ^6.2.0
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  file_picker: ^8.1.0
  spreadsheet_decoder: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
